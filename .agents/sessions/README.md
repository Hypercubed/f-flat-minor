# Sessions

Task-closeout bundles go here as gitignored subfolders, for example:

```text
YYYYMMDD-HHMMSS-short-topic/
```

Only this file is tracked. Everything else under `.agents/sessions/` stays local
working memory until it is distilled into durable docs under `.agents/`.
