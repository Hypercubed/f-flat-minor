# Bootstrap

Canonical kit scaffold templates (`AGENTS.md`, `docs/*`, `playbooks/README.md`, `sessions/README.md`) live under **learning-distill**:

`.agents/skills/learning-distill/bootstrap/`

Run **learning-distill** skill initialization before **knowledge-lint** initialization (see each skill’s `SKILL.md`). If you only have files from **knowledge-lint**, add **learning-distill** or copy missing templates from that path.
