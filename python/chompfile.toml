version = 0.1
default-task = 'build'

extensions = ['../cmp-extension.js']

[env]
RED = '\033[0;31m'
BLUE = '\033[0;34m'
BIN = './execute.py'
NC = '\033[0m'
GRN = '\033[0;32m'
CHECK = 'printf "  ${GRN}OK${NC} " || printf "  ${RED}FAIL${NC} "'

[[task]]
name = 'interpret'
run = 'cat ../ff/example.ff | ${{BIN}}'
template = 'cmp'
[task.template-options]
cmp = '../ff/example.out'

[[task]]
name = 'test'
deps = [
  ':interpret',
  ':test:out',
  ':test:tap'
]
serial = true
display = 'none'

[[task]]
name = 'test:tap'
serial = true
invalidation = 'always'
validation = 'none'
run = '''
  shopt -s nullglob
  shopt -s extglob
  shopt -s globstar
  prove --exec '../shell/ff-run.sh --quiet --run python' ../ff/**/*.test.ffp
'''

[[task]]
name = 'test:out'
deps = [
    '../ff/##.out',
]
serial = true
invalidation = 'always'
validation = 'none'
display = 'status-only'
run = '''
  if [ -f ${DEP%.*}.ffp ]; then
    ../shell/ff-run.sh --quiet --run python ${DEP%.*}.ffp | diff --ignore-trailing-space -q - $DEP && ${{CHECK}};
  fi
  if [ -f ${DEP%.*}.ff ]; then
    cat ${DEP%.*}.ff | ${{BIN}} | diff --ignore-trailing-space -q - $DEP && ${{CHECK}};
  fi
'''
