{
  "name": "f-flat-minor-web",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist -b gh-pages"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "typescript": "^5.8.2",
    "vite": "^6.2.0"
  }
}
